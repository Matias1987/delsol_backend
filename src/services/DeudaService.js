const deudasDB = require("../database/Deuda");

const agregarDeuda = (params, callback) => {};

const eliminarDeuda = (params, callback) => {};

const listarDeudas = (params, callback) => {};

const detalleDeuda = (params, callback) => {};

module.exports = {
  agregarDeuda,
  eliminarDeuda,
  listarDeudas,
  detalleDeuda,
};
