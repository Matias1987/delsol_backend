const { doQuery, escapeHelper } = require("./helpers/queriesHelper");

const agregar_sucursal_ip = (data, callback) => {
    const query = ``;
    doQuery(query,(response)=>{
        callback(response)
    })
}

const validar_sucursal_ip = (data, callback) => {
    const query = ``;
    doQuery(query,(response)=>{
        callback(response)
    })
}

const obtener_sucursal_ip = (data, callback) => {
    const query = ``;
    doQuery(query,(response)=>{
        callback(response)
    })
}

module.exports = {
    agregar_sucursal_ip,
    validar_sucursal_ip,
    obtener_sucursal_ip,
}